<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @publication.title %>
</p>

<% @publication_attachments.each do |p| %>
    <%= image_tag p.image_url %>
    <%= link_to "Edit Attachment", edit_publication_attachment(p)%>
<% end %>

<%= link_to 'Edit', edit_publication_path(@publication) %> |